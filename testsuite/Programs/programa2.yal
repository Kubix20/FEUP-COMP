module programa2 {
y[];
function a[]=f1(b[]) {
		 i = 0;
		 N = b.size;
		 a = [N];
		 while(i<b.size) {
		 	a[i] = b[i];
		 	i = i +1;
		 }
}

function f2(N) {
		 y = [N];

		 y = 1; // initialize all the N elements of a with value 1
}

function main() {
	b = [100];
	b[0] = 1;
	b[99] = 2;

	a = f1(b);

	first = a[0];
	last = a[99];
	io.println("first: ",first);
	io.println("last: ",last);

	f2(100);

	first = y[0];
	last = y[99];
	io.println("first: ",first);
	io.println("last: ",last);
}
}
